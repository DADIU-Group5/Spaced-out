using UnityEngine;
using System.Collections;

public class FloatingProps : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log("NYI. Destroys the object.");
        Destroy(gameObject);
	}
}
